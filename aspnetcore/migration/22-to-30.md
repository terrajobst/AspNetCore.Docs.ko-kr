---
title: ASP.NET Core에서 migrate 2.2-3.0 미리 보기
author: tdykstra
description: ASP.NET Core 3.0에는 ASP.NET Core 2.2 프로젝트를 마이그레이션하는 방법에 알아봅니다.
ms.author: tdykstra
ms.custom: mvc
ms.date: 04/17/2019
uid: migration/22-to-30
ms.openlocfilehash: 59e8fc4da0d2ce5a2a3ab9e4141b84af0da4b771
ms.sourcegitcommit: eb784a68219b4829d8e50c8a334c38d4b94e0cfa
ms.translationtype: MT
ms.contentlocale: ko-KR
ms.lasthandoff: 04/22/2019
ms.locfileid: "59982909"
---
# <a name="migrate-from-aspnet-core-22-to-30"></a><span data-ttu-id="b23d7-103">ASP.NET Core 2.2-3.0에서 마이그레이션</span><span class="sxs-lookup"><span data-stu-id="b23d7-103">Migrate from ASP.NET Core 2.2 to 3.0</span></span>

<span data-ttu-id="b23d7-104">하 여 [Scott Addie](https://github.com/scottaddie) 고 [Rick Anderson](https://twitter.com/RickAndMSFT)</span><span class="sxs-lookup"><span data-stu-id="b23d7-104">By [Scott Addie](https://github.com/scottaddie) and [Rick Anderson](https://twitter.com/RickAndMSFT)</span></span>

<span data-ttu-id="b23d7-105">이 문서에서는 ASP.NET Core 3.0 기존 ASP.NET Core 2.2 프로젝트를 업데이트 하는 방법에 설명 합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-105">This article explains how to update an existing ASP.NET Core 2.2 project to ASP.NET Core 3.0.</span></span>

[!INCLUDE[](~/includes/net-core-prereqs-all-3.0.md)]

## <a name="update-the-project-file"></a><span data-ttu-id="b23d7-106">프로젝트 파일을 업데이트</span><span class="sxs-lookup"><span data-stu-id="b23d7-106">Update the project file</span></span>

* <span data-ttu-id="b23d7-107">설정 된 [대상 프레임 워크 모니커 (TFM)](/dotnet/standard/frameworks#referring-to-frameworks) 에 `netcoreapp3.0`:</span><span class="sxs-lookup"><span data-stu-id="b23d7-107">Set the [Target Framework Moniker (TFM)](/dotnet/standard/frameworks#referring-to-frameworks) to `netcoreapp3.0`:</span></span>

  ```xml
  <TargetFramework>netcoreapp3.0</TargetFramework>
  ```

* <span data-ttu-id="b23d7-108">제거 `<PackageReference>` 에 [Microsoft.AspNetCore.All](xref:fundamentals/metapackage) 하거나 [Microsoft.AspNetCore.App](xref:fundamentals/metapackage-app) 메타 패키지입니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-108">Remove any `<PackageReference>` to the [Microsoft.AspNetCore.All](xref:fundamentals/metapackage) or [Microsoft.AspNetCore.App](xref:fundamentals/metapackage-app) metapackage.</span></span>

* <span data-ttu-id="b23d7-109">업데이트를 `Version` 에서 남은 `<PackageReference>` 요소에 대 한 `Microsoft.AspNetCore.*` 현재 미리 보기 패키지 (예를 들어 `3.0.0-preview-18579-0053`).</span><span class="sxs-lookup"><span data-stu-id="b23d7-109">Update the `Version` on remaining `<PackageReference>` elements for `Microsoft.AspNetCore.*` packages to the current preview (for example, `3.0.0-preview-18579-0053`).</span></span>

  <span data-ttu-id="b23d7-110">3.0 패키지 버전이 없으면 패키지 있습니다에서 사용 되지 3.0.</span><span class="sxs-lookup"><span data-stu-id="b23d7-110">If there's no 3.0 version of a package, the package might have been deprecated in 3.0.</span></span> <span data-ttu-id="b23d7-111">일부인 이러한 많은 `Microsoft.AspNetCore.App` 개별적으로 참조할 수 없습니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-111">Many of these are part of `Microsoft.AspNetCore.App` and shouldn't be referenced individually.</span></span> <span data-ttu-id="b23d7-112">3.0에 더 이상 생성 된 패키지 목록을 예비 참조 [3.0에서 공유 프레임 워크 어셈블리에 대 한 패키지를 생성 중지 (aspnet/AspNetCore #3756)](https://github.com/aspnet/AspNetCore/issues/3756)합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-112">For a preliminary list of packages no longer produced in 3.0, see [Stop producing packages for shared framework assemblies in 3.0 (aspnet/AspNetCore #3756)](https://github.com/aspnet/AspNetCore/issues/3756).</span></span>

* <span data-ttu-id="b23d7-113">일부 어셈블리에서 제거 된 `Microsoft.AspNetCore.App` 2.x에서 3.0 사이입니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-113">Some assemblies were removed from `Microsoft.AspNetCore.App` between 2.x and 3.0.</span></span> <span data-ttu-id="b23d7-114">추가 해야 할 수 있습니다 `<PackageReference>` 에 나열 된 패키지에서 Api를 사용 하는 경우 항목 [Microsoft.AspNetCore.App 3.0에서 제거할 어셈블리 (aspnet/AspNetCore #3755)](https://github.com/aspnet/AspNetCore/issues/3755)합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-114">You may need to add `<PackageReference>` items if you're using APIs from packages listed in [Assemblies being removed from Microsoft.AspNetCore.App 3.0 (aspnet/AspNetCore #3755)](https://github.com/aspnet/AspNetCore/issues/3755).</span></span>

  <span data-ttu-id="b23d7-115">예를 들어 `Microsoft.EntityFrameworkCore` 하 고 `System.Data.SqlClient` 는 더 이상 속하지 `Microsoft.AspNetCore.App`합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-115">For example, `Microsoft.EntityFrameworkCore` and `System.Data.SqlClient` are no longer part of `Microsoft.AspNetCore.App`.</span></span> <span data-ttu-id="b23d7-116">에 전달 하는 어셈블리 목록을 `Microsoft.AspNetCore.App` 아직 완성 되지 않았습니다 및 3.0 RTM 이전 변경 됩니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-116">The list of assemblies shipping in `Microsoft.AspNetCore.App` hasn't been finalized yet and will change before 3.0 RTM.</span></span>

* <span data-ttu-id="b23d7-117">추가 [Json.NET 지원](#jsonnet-support)합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-117">Add [Json.NET support](#jsonnet-support).</span></span>

* <span data-ttu-id="b23d7-118">기본적으로 프로젝트를 [-in-process 호스팅 모델](xref:host-and-deploy/aspnet-core-module#in-process-hosting-model) ASP.NET Core 3.0에서 이상.</span><span class="sxs-lookup"><span data-stu-id="b23d7-118">Projects default to the [in-process hosting model](xref:host-and-deploy/aspnet-core-module#in-process-hosting-model) in ASP.NET Core 3.0 or later.</span></span> <span data-ttu-id="b23d7-119">제거할 수도 있습니다는 `<AspNetCoreHostingModel>` 속성의 값이 프로젝트 파일에서 `InProcess`합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-119">You may optionally remove the `<AspNetCoreHostingModel>` property in the project file if its value is `InProcess`.</span></span>

## <a name="jsonnet-support"></a><span data-ttu-id="b23d7-120">Json.NET 지원</span><span class="sxs-lookup"><span data-stu-id="b23d7-120">Json.NET support</span></span>

<span data-ttu-id="b23d7-121">작업의 일부로 [ASP.NET Core 공유 프레임 워크 향상](https://blogs.msdn.microsoft.com/webdev/2018/10/29/a-first-look-at-changes-coming-in-asp-net-core-3-0/)를 [Json.NET](https://www.newtonsoft.com/json/help/html/Introduction.htm) ASP.NET Core 공유 프레임 워크에서 제거 되었습니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-121">As part of the work to [improve the ASP.NET Core shared framework](https://blogs.msdn.microsoft.com/webdev/2018/10/29/a-first-look-at-changes-coming-in-asp-net-core-3-0/), [Json.NET](https://www.newtonsoft.com/json/help/html/Introduction.htm) has been removed from the ASP.NET Core shared framework.</span></span>

<span data-ttu-id="b23d7-122">에 ASP.NET Core 3.0 프로젝트에서 Json.NET을 사용 합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-122">To use Json.NET in an ASP.NET Core 3.0 project:</span></span>

* <span data-ttu-id="b23d7-123">에 대 한 패키지 참조 추가 [Microsoft.AspNetCore.Mvc.NewtonsoftJson](https://nuget.org/packages/Microsoft.AspNetCore.Mvc.NewtonsoftJson)합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-123">Add a package reference to [Microsoft.AspNetCore.Mvc.NewtonsoftJson](https://nuget.org/packages/Microsoft.AspNetCore.Mvc.NewtonsoftJson).</span></span>
* <span data-ttu-id="b23d7-124">업데이트 `Startup.ConfigureServices` 호출할 `AddNewtonsoftJson()`합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-124">Update `Startup.ConfigureServices` to call `AddNewtonsoftJson()`.</span></span>

  ```csharp
  services.AddMvc()
      .AddNewtonsoftJson();
  ```

  <span data-ttu-id="b23d7-125">Newtonsoft 설정을 사용 하 여 설정할 수 있습니다 `AddNewtonsoftJson`:</span><span class="sxs-lookup"><span data-stu-id="b23d7-125">Newtonsoft settings can be set with `AddNewtonsoftJson`:</span></span>

  ```csharp
  services.AddMvc()
      .AddNewtonsoftJson(options =>
             options.SerializerSettings.ContractResolver =
                new CamelCasePropertyNamesContractResolver());
  ```

## <a name="mvc-service-registration"></a><span data-ttu-id="b23d7-126">MVC 서비스 등록</span><span class="sxs-lookup"><span data-stu-id="b23d7-126">MVC service registration</span></span>

<span data-ttu-id="b23d7-127">내 MVC 시나리오를 등록 하기 위한 새로운 옵션을 추가 하는 ASP.NET Core 3.0 `Startup.ConfigureServices`합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-127">ASP.NET Core 3.0 adds new options for registering MVC scenarios inside `Startup.ConfigureServices`.</span></span>

<span data-ttu-id="b23d7-128">세 가지 새로운 최상위 확장명 메서드와 관련 된 MVC 시나리오에서 `IServiceCollection` 사용할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-128">Three new top-level extension methods related to MVC scenarios on `IServiceCollection` are available.</span></span> <span data-ttu-id="b23d7-129">대신 이러한 새 메서드를 사용 하는 템플릿 `UseMvc`합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-129">Templates use these new methods instead of `UseMvc`.</span></span> <span data-ttu-id="b23d7-130">그러나 `AddMvc` 이전 릴리스에서 동일 하 게 계속 합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-130">However, `AddMvc` continues to behave as it has in previous releases.</span></span>

<span data-ttu-id="b23d7-131">다음 예제에서는 컨트롤러 및 API 관련 기능을 하지만 하지 뷰 또는 페이지에 대 한 지원을 추가합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-131">The following example adds support for controllers and API-related features, but not views or pages.</span></span> <span data-ttu-id="b23d7-132">API 템플릿에서이 코드를 사용합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-132">The API template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllers();
}
```

<span data-ttu-id="b23d7-133">다음 예제에서는 컨트롤러, API 관련 기능 및 뷰 하지 페이지에 대 한 지원을 추가합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-133">The following example adds support for controllers, API-related features, and views, but not pages.</span></span> <span data-ttu-id="b23d7-134">이 코드를 사용 하는 웹 응용 프로그램 (MVC) 템플릿:</span><span class="sxs-lookup"><span data-stu-id="b23d7-134">The Web Application (MVC) template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllersWithViews();
}
```

<span data-ttu-id="b23d7-135">다음 예에서는 최소한의 컨트롤러 및 Razor 페이지 지원을 추가합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-135">The following example adds support for Razor Pages and minimal controller support.</span></span> <span data-ttu-id="b23d7-136">웹 응용 프로그램 템플릿에이 코드를 사용합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-136">The Web Application template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddRazorPages();
}
```

<span data-ttu-id="b23d7-137">또한 새로운 메서드를 결합할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-137">The new methods can also be combined.</span></span> <span data-ttu-id="b23d7-138">다음 예제에서는 호출에 해당 하는 `AddMvc` ASP.NET Core 2.2에서:</span><span class="sxs-lookup"><span data-stu-id="b23d7-138">The following example is equivalent to calling `AddMvc` in ASP.NET Core 2.2:</span></span> 

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllers();
    services.AddRazorPages();
}
```

## <a name="update-routing-startup-code"></a><span data-ttu-id="b23d7-139">라우팅 시작 코드를 업데이트 합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-139">Update routing startup code</span></span>

<span data-ttu-id="b23d7-140">앱을 호출 하는 경우 `UseMvc` 또는 `UseSignalR`, 앱을 마이그레이션할 [끝점에 라우팅](xref:fundamentals/routing) 가능한 경우.</span><span class="sxs-lookup"><span data-stu-id="b23d7-140">If an app calls `UseMvc` or `UseSignalR`, migrate the app to [Endpoint Routing](xref:fundamentals/routing) if possible.</span></span> <span data-ttu-id="b23d7-141">이전 버전의 MVC 사용 하 여 끝점에 라우팅 호환성을 높이기 위해 ASP.NET Core 2.2에 도입 된 URL 생성에 일부 변경 내용이 되돌릴 했습니다 했습니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-141">To improve Endpoint Routing compatibility with previous versions of MVC, we've reverted some of the changes in URL generation introduced in ASP.NET Core 2.2.</span></span> <span data-ttu-id="b23d7-142">라우팅 끝점을 사용 하 여 2.2에 문제 발생 하는 경우 다음 예외를 사용 하 여 ASP.NET Core 3.0의 향상 된 기능 예상:</span><span class="sxs-lookup"><span data-stu-id="b23d7-142">If you experienced problems using Endpoint Routing in 2.2, expect improvements in ASP.NET Core 3.0 with the following exceptions:</span></span>

* <span data-ttu-id="b23d7-143">앱을 구현 하는 경우 `IRouter` 에서 상속 하거나 `Route`, 지금은 마이그레이션 방지 하는 것이 좋습니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-143">If the app implements `IRouter` or inherits from `Route`, you may want to avoid migrating at this time.</span></span> <span data-ttu-id="b23d7-144">에 피드백을 주시기 [IRouter 기반 구현을 끝점 라우팅에 마이그레이션하도록 계획](https://github.com/aspnet/AspNetCore/issues/4221)합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-144">Provide feedback at [Plan to migrate IRouter based implementations onto Endpoint Routing](https://github.com/aspnet/AspNetCore/issues/4221).</span></span>

* <span data-ttu-id="b23d7-145">앱에 직접 액세스 하는 경우 `RouteData.Routers` MVC 내 지금은 마이그레이션 방지 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-145">If the app directly accesses `RouteData.Routers` inside MVC, you may want to avoid migrating at this time.</span></span> <span data-ttu-id="b23d7-146">에 피드백을 주시기 [RouteData.Routers 사용에 대 한 마이그레이션 지침](https://github.com/aspnet/AspNetCore/issues/9148)합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-146">Provide feedback at [Migration guidance for using RouteData.Routers](https://github.com/aspnet/AspNetCore/issues/9148).</span></span>

<span data-ttu-id="b23d7-147">동일한 경로 패턴 구문 및 기능을 작성 하는 경로 패턴 끝점 라우팅 지원 `IRouter`합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-147">Endpoint Routing supports the same route pattern syntax and route pattern authoring features as `IRouter`.</span></span> <span data-ttu-id="b23d7-148">끝점 라우팅 지원 `IRouteContraint`합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-148">Endpoint Routing supports `IRouteContraint`.</span></span> <span data-ttu-id="b23d7-149">끝점 라우팅 지원 `[Route]`, `[HttpGet]`, 및 기타 MVC 라우팅 특성입니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-149">Endpoint routing supports `[Route]`, `[HttpGet]`, and the other MVC routing attributes.</span></span>

<span data-ttu-id="b23d7-150">대부분의 응용 프로그램의 경우만 `Startup` 변경 해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-150">For most applications, only `Startup` requires changes.</span></span>

### <a name="migrate-startupconfigure"></a><span data-ttu-id="b23d7-151">Startup.Configure 마이그레이션</span><span class="sxs-lookup"><span data-stu-id="b23d7-151">Migrate Startup.Configure</span></span>

<span data-ttu-id="b23d7-152">일반적인 조언을:</span><span class="sxs-lookup"><span data-stu-id="b23d7-152">General advice:</span></span>

* <span data-ttu-id="b23d7-153">`UseRouting`를 추가합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-153">Add `UseRouting`.</span></span> 
* <span data-ttu-id="b23d7-154">앱을 호출 하는 경우 `UseStaticFiles`를 배치할 `UseStaticFiles` **하기 전에** `UseRouting`합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-154">If the app calls `UseStaticFiles`, place `UseStaticFiles` **before** `UseRouting`.</span></span>
* <span data-ttu-id="b23d7-155">앱과 같은 인증/권한 부여 기능을 사용 하는 경우 `AuthorizePage` 또는 `[Authorize]`에 대 한 호출을 배치 `UseAuthentication` 하 고 `UseAuthorization` **후** `UseRouting`합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-155">If the app uses authentication/authorization features such as `AuthorizePage` or `[Authorize]`, place the call to `UseAuthentication` and `UseAuthorization` **after** `UseRouting`.</span></span>
* <span data-ttu-id="b23d7-156">앱을 사용 하는 경우 [CORS](xref:security/cors) 같은 기능 `[EnableCors]`, 배치 `UseCors` 다음입니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-156">If the app uses [CORS](xref:security/cors) features, such as `[EnableCors]`, place `UseCors` next.</span></span>
* <span data-ttu-id="b23d7-157">바꿉니다 `UseMvc` 나 `UseSignalR` 사용 하 여 `UseEndpoints`입니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-157">Replace `UseMvc` or `UseSignalR` with `UseEndpoints`.</span></span>

<span data-ttu-id="b23d7-158">다음은 예가 `Startup.Configure` 일반적인 ASP.NET Core 2.2 앱에서:</span><span class="sxs-lookup"><span data-stu-id="b23d7-158">The following is an example of `Startup.Configure` in a typical ASP.NET Core 2.2 app:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseStaticFiles();
    
    app.UseAuthentication();

    app.UseSignalR(hubs =>
    {
        hubs.MapHub<ChatHub>("/chat");
    });

    app.UseMvc(routes =>
    {
        routes.MapRoute("default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

<span data-ttu-id="b23d7-159">이전 업데이트 한 후 `Startup.Configure` 코드:</span><span class="sxs-lookup"><span data-stu-id="b23d7-159">After updating the previous `Startup.Configure` code:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseStaticFiles();
    
    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();
    app.UseCors();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHub<ChatHub>("/chat");
        endpoints.MapControllerRoute("default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

### <a name="security-middleware-guidance"></a><span data-ttu-id="b23d7-160">보안 미들웨어 지침</span><span class="sxs-lookup"><span data-stu-id="b23d7-160">Security middleware guidance</span></span>

<span data-ttu-id="b23d7-161">권한 부여 및 CORS 지원 해결 통합은 [미들웨어](xref:fundamentals/middleware/index) 접근 방식입니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-161">Support for authorization and CORS is unified around the [middleware](xref:fundamentals/middleware/index) approach.</span></span> <span data-ttu-id="b23d7-162">이 이러한 시나리오에서 같은 미들웨어 및 기능을 사용할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-162">This allows use of the same middleware and functionality across these scenarios.</span></span> <span data-ttu-id="b23d7-163">이 릴리스에서 업데이트 된 인증 미들웨어를 제공 됩니다 및 CORS 미들웨어는 MVC 컨트롤러에서 사용 된 특성을 이해할 수 있도록 향상 되었습니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-163">An updated authorization middleware is provided in this release, and CORS middleware is enhanced so that it can understand the attributes used by MVC controllers.</span></span>

#### <a name="cors"></a><span data-ttu-id="b23d7-164">CORS</span><span class="sxs-lookup"><span data-stu-id="b23d7-164">CORS</span></span>

<span data-ttu-id="b23d7-165">이전에 CORS 구성 하기가 어려울 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-165">Previously, CORS could be difficult to configure.</span></span> <span data-ttu-id="b23d7-166">일부 사용 사례, 하지만 MVC 필터 된 사용 하기 위한 미들웨어 제공한 **없이** 다른 미들웨어 사용 사례입니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-166">Middleware was provided for use in some use cases, but MVC filters were intended to be used **without** the middleware in other use cases.</span></span> <span data-ttu-id="b23d7-167">ASP.NET Core 3.0을 사용 하 여 CORS를 필요로 하는 모든 앱 끝점 라우팅을 사용 하 여 동시에 CORS 미들웨어를 사용 하는 것이 좋습니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-167">With ASP.NET Core 3.0, we recommend that all apps that require CORS use the CORS middleware in tandem with Endpoint Routing.</span></span> <span data-ttu-id="b23d7-168">`UseCors` 기본 정책을 사용 하 여 제공할 수 있습니다 하 고 `[EnableCors]` 및 `[DisableCors]` 특성 기본 정책 재정의를 사용할 수 필요 합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-168">`UseCors` can be provided with a default policy, and `[EnableCors]` and `[DisableCors]` attributes can be used to override the default policy where required.</span></span> 

<span data-ttu-id="b23d7-169">다음 예제에서는</span><span class="sxs-lookup"><span data-stu-id="b23d7-169">In the following example:</span></span>

* <span data-ttu-id="b23d7-170">CORS가 사용 하 여 모든 끝점에 사용 되는 `default` 정책 이라는 합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-170">CORS is enabled for all endpoints with the `default` named policy.</span></span>
* <span data-ttu-id="b23d7-171">합니다 `MyController` 클래스에서 CORS를 사용 하지 않도록 설정 된 `[DisableCors]` 특성입니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-171">The `MyController` class disables CORS with the `[DisableCors]` attribute.</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseCors("default"); 

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

[DisableCors]
public class MyController : ControllerBase
{
    ...
}
```

#### <a name="authorization"></a><span data-ttu-id="b23d7-172">권한 부여</span><span class="sxs-lookup"><span data-stu-id="b23d7-172">Authorization</span></span>

<span data-ttu-id="b23d7-173">이전 버전의 ASP.NET Core에서 권한 부여 지원을 통해 제공 되었습니다는 `[Authorize]` 특성입니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-173">In earlier versions of ASP.NET Core, authorization support was provided via the `[Authorize]` attribute.</span></span> <span data-ttu-id="b23d7-174">인증 미들웨어를 사용할 수 없는 합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-174">Authorization middleware wasn't available.</span></span> <span data-ttu-id="b23d7-175">ASP.NET Core 3.0에서는 인증 미들웨어는 필요 합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-175">In ASP.NET Core 3.0, authorization middleware is required.</span></span> <span data-ttu-id="b23d7-176">인증 미들웨어를 배치 하는 것이 좋습니다 (`UseAuthentication`) 바로 앞 `UseAuthorization`합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-176">We recommend placing the authorization middleware (`UseAuthentication`) immediately before `UseAuthorization`.</span></span> <span data-ttu-id="b23d7-177">재정의 될 수 있는 기본 정책을 사용 하 여 인증 미들웨어를 구성할 수도 있습니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-177">Authorization middleware can also be configured with a default policy, which can be overridden.</span></span>

<span data-ttu-id="b23d7-178">ASP.NET Core 3.0 이상에서는 `UseAuthorization` 라고 `Startup.Configure`, 및 다음 `HomeController` 사용자에서 로그인 필요:</span><span class="sxs-lookup"><span data-stu-id="b23d7-178">In ASP.NET Core 3.0 or later, `UseAuthorization` is called in `Startup.Configure`, and the following `HomeController` requires a signed in user:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

public class HomeController : ControllerBase
{
    [Authorize]
    public IActionResult BuyWidgets()
    {
        ...
    }
}
```

<span data-ttu-id="b23d7-179">앱을 사용 하는 경우는 `AuthorizeFilter` 하는 정책을 제공 하는 코드를 리팩터링 좋습니다 MVC에서 글로벌 필터로 `UseAuthorization` 미들웨어입니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-179">If the app uses an `AuthorizeFilter` as a global filter in MVC, we recommend refactoring the code to provide a policy to the `UseAuthorization` middleware.</span></span>

<span data-ttu-id="b23d7-180">다음 예제에서는 사용자 지정 정책을 적용할 모든 요청 될 때 `UseAuthorization` 가 호출 및 `HomeController` 앱에 로그인 하는 사용자 없이 액세스할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-180">In the following example, a custom policy to be applied to all requests when `UseAuthorization` is called, and the `HomeController` allows access without the user signing into the app:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization(new AuthorizationPolicyBuilder().Build()));

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

[AllowAnonymous]
public class HomeController : ControllerBase
{
    ...
}
```

<span data-ttu-id="b23d7-181">미들웨어에서 권한 부여 프레임 워크 특정 권한 부여가 없이 작동 합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-181">Authorization by middleware works without the framework having any specific knowledge of authorization.</span></span> <span data-ttu-id="b23d7-182">예를 들어 [상태 검사](xref:host-and-deploy/health-checks) 특정 권한 부여가 있지만 상태 검사는 미들웨어에서 적용 되는 구성 가능한 권한 부여 정책이 있을 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-182">For instance, [health checks](xref:host-and-deploy/health-checks) has no specific knowledge of authorization, but health checks can have a configurable authorization policy applied by the middleware.</span></span>

<span data-ttu-id="b23d7-183">다음 예에서 `UseAuthorization` 기본 정책이 없는 권한 부여를 처리 하지만 `/healthz` 상태 검사 엔드포인트에서 사용할 필요는 `admin` 역할:</span><span class="sxs-lookup"><span data-stu-id="b23d7-183">In the following example, `UseAuthorization` processes authorization without a default policy, but the `/healthz` health check endpoint requires the user to be in the `admin` role:</span></span> 

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints
            .MapHealthChecks("/healthz")
            .RequireAuthorization(new AuthorizeAttribute(){ Roles = "admin", });
    });
}
```

<span data-ttu-id="b23d7-184">일부 시나리오에 대 한 보호를 구현 됩니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-184">Protection is implemented for some scenarios.</span></span> <span data-ttu-id="b23d7-185">`UseEndpoint` 누락 된 미들웨어는 권한 부여 또는 CORS 정책의 건너뛰었습니다 하는 경우 미들웨어에서 예외를 throw 합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-185">`UseEndpoint` middleware throws an exception if an authorization or CORS policy is skipped due to missing middleware.</span></span> <span data-ttu-id="b23d7-186">진행 중 잘못 된 구성에 대 한 추가 피드백을 제공 하기 위한 지원이 분석기.</span><span class="sxs-lookup"><span data-stu-id="b23d7-186">Analyzer support to provide additional feedback about misconfiguration is in progress.</span></span>

### <a name="migrate-signalr"></a><span data-ttu-id="b23d7-187">SignalR 마이그레이션</span><span class="sxs-lookup"><span data-stu-id="b23d7-187">Migrate SignalR</span></span>

<span data-ttu-id="b23d7-188">SignalR 허브의 매핑을 이제 내에서 수행 됩니다 `UseEndpoints`합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-188">Mapping of SignalR hubs now takes place inside `UseEndpoints`.</span></span> 

<span data-ttu-id="b23d7-189">각 허브를 사용 하 여 매핑 `MapHub`합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-189">Map each hub with `MapHub`.</span></span> <span data-ttu-id="b23d7-190">이전 버전에서와 같이 각 허브 명시적으로 나열 됩니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-190">As in previous versions, each hub is explicitly listed.</span></span>

<span data-ttu-id="b23d7-191">다음 예제에서에 대 한 지원의 `ChatHub` SignalR 허브 추가 됩니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-191">In the following example, support for the `ChatHub` SignalR hub is added:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHub<ChatHub>();
    });
}
```

### <a name="migrate-mvc-controllers"></a><span data-ttu-id="b23d7-192">MVC 컨트롤러 마이그레이션</span><span class="sxs-lookup"><span data-stu-id="b23d7-192">Migrate MVC controllers</span></span>

<span data-ttu-id="b23d7-193">이제 매핑 컨트롤러 내에서 수행 됩니다 `UseEndpoints`합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-193">Mapping of controllers now takes place inside `UseEndpoints`.</span></span> 

<span data-ttu-id="b23d7-194">추가 `MapControllers` 앱 특성 라우팅을 사용 하는 경우.</span><span class="sxs-lookup"><span data-stu-id="b23d7-194">Add `MapControllers` if the app uses attribute routing.</span></span> <span data-ttu-id="b23d7-195">라우팅 포함 많은 프레임 워크는 ASP.NET Core 3.0에서 이상에 대 한 지원, 컨트롤러 특성 라우팅됩니다 추가 되므로 옵트인 합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-195">Since routing includes support for many frameworks in ASP.NET Core 3.0 or later, adding attribute-routed controllers is opt-in.</span></span> 

<span data-ttu-id="b23d7-196">다음으로 바꿉니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-196">Replace the following:</span></span>

* <span data-ttu-id="b23d7-197">`MapRoute` 사용 하 여 `MapControllerRoute`</span><span class="sxs-lookup"><span data-stu-id="b23d7-197">`MapRoute` with `MapControllerRoute`</span></span>
* <span data-ttu-id="b23d7-198">`MapAreaRoute` 사용 하 여 `MapAreaControllerRoute`</span><span class="sxs-lookup"><span data-stu-id="b23d7-198">`MapAreaRoute` with `MapAreaControllerRoute`</span></span>

<span data-ttu-id="b23d7-199">이제 라우팅 이상의 MVC에 대 한 지원을 포함 하므로 용어 용도 분명히 이러한 메서드 수 있도록 변경 되었습니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-199">Since routing now includes support for more than just MVC, the terminology has changed to make these methods clearly state what they do.</span></span> <span data-ttu-id="b23d7-200">규칙 기반 경로와 같은 `MapControllerRoute` / `MapAreaControllerRoute` / `MapDefaultControllerRoute` 추가 되는 순서 대로 적용 됩니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-200">Conventional routes such as `MapControllerRoute`/`MapAreaControllerRoute`/`MapDefaultControllerRoute` are applied in the order that they're added.</span></span> <span data-ttu-id="b23d7-201">보다 구체적인 경로 (예: 영역에 대 한 경로)를 먼저 배치 합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-201">Place more specific routes (such as routes for an area) first.</span></span>

<span data-ttu-id="b23d7-202">다음 예제에서는</span><span class="sxs-lookup"><span data-stu-id="b23d7-202">In the following example:</span></span>

* <span data-ttu-id="b23d7-203">`MapControllers` 컨트롤러 특성 라우팅에 대 한 지원을 추가합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-203">`MapControllers` adds support for attribute-routed controllers.</span></span>
* <span data-ttu-id="b23d7-204">`MapAreaControllerRoute` 영역에서 컨트롤러에 대 한 규칙 기반 경로 추가합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-204">`MapAreaControllerRoute` adds a conventional route for controllers in an area.</span></span>
* <span data-ttu-id="b23d7-205">`MapControllerRoute` 컨트롤러에 대 한 규칙 기반 경로 추가합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-205">`MapControllerRoute` adds a conventional route for controllers.</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapControllers();
        endpoints.MapAreaControllerRoute(
            "admin", 
            "admin", 
            "Admin/{controller=Home}/{action=Index}/{id?}");
        endpoints.MapControllerRoute(
            "default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

### <a name="migrate-razor-pages"></a><span data-ttu-id="b23d7-206">Razor 페이지로 마이그레이션</span><span class="sxs-lookup"><span data-stu-id="b23d7-206">Migrate Razor Pages</span></span>

<span data-ttu-id="b23d7-207">내에서 이루어지는 Razor 페이지를 이제 매핑 `UseEndpoints`합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-207">Mapping Razor Pages now takes place inside `UseEndpoints`.</span></span>

<span data-ttu-id="b23d7-208">추가 `MapRazorPages` 앱 Razor 페이지를 사용 하는 경우.</span><span class="sxs-lookup"><span data-stu-id="b23d7-208">Add `MapRazorPages` if the app uses Razor Pages.</span></span> <span data-ttu-id="b23d7-209">끝점에 라우팅 포함 많은 프레임 워크에 대 한 지원, Razor 페이지 추가 되므로 이제 옵트인 합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-209">Since Endpoint Routing includes support for many frameworks, adding Razor Pages is now opt-in.</span></span>

<span data-ttu-id="b23d7-210">다음 예에서 `MapRazorPages` Razor 페이지에 대 한 지원이 추가 되었습니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-210">In the following example, `MapRazorPages` adds support for Razor Pages:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapRazorPages();
    });
}
```

### <a name="use-mvc-without-endpoint-routing"></a><span data-ttu-id="b23d7-211">MVC를 사용 하 여 끝점에 라우팅 하지 않고</span><span class="sxs-lookup"><span data-stu-id="b23d7-211">Use MVC without Endpoint Routing</span></span>

<span data-ttu-id="b23d7-212">통해 MVC를 사용 하 여 `UseMvc` 또는 `UseMvcWithDefaultRoute` ASP.NET Core 3.0 필요 명시적인 옵트인 내 `Startup.ConfigureServices`합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-212">Using MVC via `UseMvc` or `UseMvcWithDefaultRoute` in ASP.NET Core 3.0 requires an explicit opt-in inside `Startup.ConfigureServices`.</span></span> <span data-ttu-id="b23d7-213">MVC를 초기화 하는 동안 권한 부여 및 CORS 미들웨어에서 사용할 수 있습니다 있는지 여부를 알아야 하기 때문에 이것이 필요 합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-213">This is required because MVC must know whether it can rely on the authorization and CORS middleware during initialization.</span></span> <span data-ttu-id="b23d7-214">분석기에는 앱을 지원 되지 않는 구성을 사용 하려고 하는 경우 경고를 표시 하는 제공 됩니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-214">An analyzer is provided that warns if the app attempts to use an unsupported configuration.</span></span>

<span data-ttu-id="b23d7-215">레거시 앱이 필요한 경우 `IRouter` 지원, 사용 안 함 `EnableEndpointRouting` 방법을 다음 중 하나를 사용 하 여 `Startup.ConfigureServices`:</span><span class="sxs-lookup"><span data-stu-id="b23d7-215">If the app requires legacy `IRouter` support, disable `EnableEndpointRouting` using any of the following approaches in `Startup.ConfigureServices`:</span></span>

```csharp
services.AddMvc(options => options.EnableEndpointRouting = false);
```

```csharp
services.AddControllers(options => options.EnableEndpointRouting = false);
```

```csharp
services.AddControllersWithViews(options => options.EnableEndpointRouting = false);

```

```csharp
services.AddRazorPages().AddMvcOptions(options => options.EnableEndpointRouting = false);
```

### <a name="migrate-health-checks"></a><span data-ttu-id="b23d7-216">마이그레이션 상태 확인</span><span class="sxs-lookup"><span data-stu-id="b23d7-216">Migrate health checks</span></span>

<span data-ttu-id="b23d7-217">상태 검사로 사용할 수 있습니다는 *라우터 웨어* 끝점 라우팅을 사용 합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-217">Health checks can be used as a *router-ware* with Endpoint Routing.</span></span>

<span data-ttu-id="b23d7-218">추가 `MapHealthChecks` 상태를 사용 하려면 끝점 라우팅을 사용 하 여 확인 합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-218">Add `MapHealthChecks` to use health checks with Endpoint Routing.</span></span> <span data-ttu-id="b23d7-219">합니다 `MapHealthChecks` 메서드에서 인수 비슷합니다 `UseHealthChecks`합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-219">The `MapHealthChecks` method accepts arguments similar to `UseHealthChecks`.</span></span> <span data-ttu-id="b23d7-220">사용 하는 이점은 `MapHealthChecks` 위에 `UseHealthChecks` 권한 부여를 적용 하 고 큰 세밀 하 게 일치 하는 정책 제어할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-220">The advantage of using `MapHealthChecks` over `UseHealthChecks` is the ability to apply authorization and to have greater fine-grained control over the matching policy.</span></span> 

<span data-ttu-id="b23d7-221">다음 예에서 `MapHealthChecks` 에서 상태 확인 끝점에 대 한 라고 `/healthz`:</span><span class="sxs-lookup"><span data-stu-id="b23d7-221">In the following example, `MapHealthChecks` is called for a health check endpoint at `/healthz`:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHealthChecks("/healthz", new HealthCheckOptions() { });
    });
}
```

## <a name="hostbuilder-replaces-webhostbuilder"></a><span data-ttu-id="b23d7-222">HostBuilder WebHostBuilder를 대체합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-222">HostBuilder replaces WebHostBuilder</span></span>

<span data-ttu-id="b23d7-223">ASP.NET Core 3.0 템플릿을 사용 하 여 [제네릭 호스트](xref:fundamentals/host/generic-host)합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-223">The ASP.NET Core 3.0 templates use [Generic Host](xref:fundamentals/host/generic-host).</span></span> <span data-ttu-id="b23d7-224">사용 하는 이전 버전과 [웹 호스트](xref:fundamentals/host/web-host)합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-224">Previous versions used [Web Host](xref:fundamentals/host/web-host).</span></span> <span data-ttu-id="b23d7-225">다음 코드에서는 생성 된 ASP.NET Core 3.0 템플릿을 `Program` 클래스:</span><span class="sxs-lookup"><span data-stu-id="b23d7-225">The following code shows the ASP.NET Core 3.0 template generated `Program` class:</span></span>

[!code-csharp[](22-to-30/samples/Program.cs?name=snippet)]

<span data-ttu-id="b23d7-226">다음 코드에서는 템플릿에서 생성 된 ASP.NET Core 2.2 `Program` 클래스:</span><span class="sxs-lookup"><span data-stu-id="b23d7-226">The following code shows the ASP.NET Core 2.2 template-generated `Program` class:</span></span>

[!code-csharp[](22-to-30/samples/Program2.2.cs?name=snippet)]

<span data-ttu-id="b23d7-227"><xref:Microsoft.AspNetCore.Hosting.IWebHostBuilder> 3.0에 남아 있으며 유형의 `webBuilder` 앞의 코드 샘플에 표시 합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-227"><xref:Microsoft.AspNetCore.Hosting.IWebHostBuilder> remains in 3.0 and is the type of the `webBuilder` seen in the preceding code sample.</span></span> <span data-ttu-id="b23d7-228"><xref:Microsoft.AspNetCore.Hosting.WebHostBuilder> 이후 릴리스에서 중단 되 고 바뀝니다 `HostBuilder`합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-228"><xref:Microsoft.AspNetCore.Hosting.WebHostBuilder> will be deprecated in a future release and replaced by `HostBuilder`.</span></span>

<span data-ttu-id="b23d7-229">가장 중요 한 변경 내용이 `WebHostBuilder` 에 `HostBuilder` 상태인 [DI (종속성 주입)](xref:fundamentals/dependency-injection)합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-229">The most significant change from `WebHostBuilder` to `HostBuilder` is in [dependency injection (DI)](xref:fundamentals/dependency-injection).</span></span> <span data-ttu-id="b23d7-230">사용 하는 경우 `HostBuilder`을 삽입할 수 있습니다 <xref:Microsoft.Extensions.Configuration.IConfiguration> 하 고 <xref:Microsoft.AspNetCore.Hosting.IHostingEnvironment> 에 `Startup`의 생성자입니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-230">When using `HostBuilder`, you can only inject <xref:Microsoft.Extensions.Configuration.IConfiguration> and <xref:Microsoft.AspNetCore.Hosting.IHostingEnvironment> into `Startup`'s constructor.</span></span> <span data-ttu-id="b23d7-231">`HostBuilder` DI 제약 조건:</span><span class="sxs-lookup"><span data-stu-id="b23d7-231">The `HostBuilder` DI constraints:</span></span>

* <span data-ttu-id="b23d7-232">한 번만 작성할 DI 컨테이너를 사용 하도록 설정 합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-232">Enable the DI container to be built only one time.</span></span>
* <span data-ttu-id="b23d7-233">단일 항목의 여러 인스턴스를 확인 하는 같은 결과 개체 수명 문제를 방지할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-233">Avoids the resulting object lifetime issues like resolving multiple instances of singletons.</span></span>

## <a name="update-signalr-code"></a><span data-ttu-id="b23d7-234">SignalR 코드 업데이트</span><span class="sxs-lookup"><span data-stu-id="b23d7-234">Update SignalR code</span></span>

<span data-ttu-id="b23d7-235">호출 하는 경우 `AddJsonProtocol`를 사용 하 여 대체 `AddNewtonsoftJsonProtocol`합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-235">If you call `AddJsonProtocol`, replace it with `AddNewtonsoftJsonProtocol`.</span></span>

* <span data-ttu-id="b23d7-236">다음 예제에서는 전과 변경 후 서버 코드를 보여 줍니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-236">The following examples show server code before and after the change:</span></span>

  ```csharp
  services.AddSignalR(...)
          .AddJsonProtocol(...) // 2.2
  ```

  ```csharp
  services.AddSignalR(...)
          .AddNewtonsoftJsonProtocol(...) // 3.0
  ```

* <span data-ttu-id="b23d7-237">다음 예제에서는 전과 변경 후.NET 클라이언트 코드를 보여 줍니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-237">The following examples show .NET client code before and after the change:</span></span>

  ```csharp
  connection = new HubConnectionBuilder()
      .WithUrl(...)
      .AddJsonProtocol(...) // 2.2
      .Build()
  ```

  ```csharp
  connection = new HubConnectionBuilder()
      .WithUrl(...)
      .AddNewtonsoftJsonProtocol(...) // 3.0
      .Build()
  ```

## <a name="opt-in-to-runtime-compilation"></a><span data-ttu-id="b23d7-238">런타임 컴파일으로 옵트인</span><span class="sxs-lookup"><span data-stu-id="b23d7-238">Opt in to runtime compilation</span></span>

<span data-ttu-id="b23d7-239">3.0에서는 런타임 컴파일은 옵트인 시나리오입니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-239">In 3.0, runtime compilation is an opt-in scenario.</span></span> <span data-ttu-id="b23d7-240">런타임 컴파일 사용 하려면 <xref:mvc/views/view-compilation#runtime-compilation>합니다.</span><span class="sxs-lookup"><span data-stu-id="b23d7-240">To enable runtime compilation, see <xref:mvc/views/view-compilation#runtime-compilation>.</span></span>
